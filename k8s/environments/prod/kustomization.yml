apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

resources:
- ../../base

patchesStrategicMerge:
- deployment-patch.yml

commonLabels:
  environment: prod

namePrefix: prod-

images:
- name: myapp
  newTag: stable